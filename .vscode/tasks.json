{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "generate",
            "type": "shell",
            "command": "pelican -o . -s publishconf.py",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "launch-http-server",
            "type": "shell",
            "command": "python3 -m http.server"
        }
    ]
}